# 记忆链 MemoryChain · TauriTavern 扩展

按实体召回的**情景记忆**：只把"当前这一章"与"命中实体的往事"送进上下文，其余全部折叠落盘。
解决长对话把上下文吃满、人物与旧事被挤出上下文的问题。

- 版本：1.3.0
- 适用：TauriTavern（也兼容 SillyTavern 前端的第三方扩展机制）
- 纯前端扩展：无后端依赖、无网络请求、不执行任何远程代码

**1.3.0 新增：剧情闸门**——按里程碑自动解锁世界书里的「【未解锁】…」条目，并同步改写「开局状态与剧情进度」，让"未来内容"只在剧情推进后出现。
**1.2.x**：词表导入三入口（选文件／读宿主世界书／粘贴 JSON）＋状态提示；词表尊重禁用条目；设置与词表全局共享；找不到设置容器时降级为浮动面板。

---

## 一、安装（TauriTavern）

### 方式 A：直接放文件夹（最快，推荐）

把本文件夹整个复制到下面任一个目录，然后**重启 TauriTavern**：

| 作用域 | 路径 |
| --- | --- |
| 仅当前用户（local，优先级高） | `data/default-user/extensions/memory-chain/` |
| 全局（global） | `data/extensions/third-party/memory-chain/` |

> 注意：local 与 global 同名时以 local 为准；目录名与扩展名不必一致，但建议保持 `memory-chain`。

### 方式 B：用扩展面板的 Git 安装

TauriTavern 的扩展安装只接受**匿名 http(s) Git 地址**（ZIP/REST 安装已被移除）。做法：

1. 把本文件夹的内容放到一个 Git 仓库的**根目录**（仓库根必须直接含 `manifest.json`，否则安装前校验会失败）；
2. 推到 GitHub / Gitee 等可匿名访问的地址；
3. 扩展面板 → 安装扩展 → 填入仓库地址（可带 `#分支` 或 `#tag`）。

---

## 二、首次配置（就一步：建词表）

展开 **记忆链 MemoryChain** 面板，第一块就是「**① 词表（世界书）——必须先做这一步**」，下面有三条路，任选其一：

| 入口 | 说明 |
| --- | --- |
| **选择世界书 JSON 文件…** | 点它选 `今山 9.json`（最稳，推荐） |
| **从当前绑定的世界书读取** | 让扩展自己向宿主要当前聊天绑定的世界书（TauriTavern/SillyTavern 提供该接口时可用） |
| **粘贴 JSON…** | 把 JSON 全文粘进文本框再点「解析并建表」，适合文件选择器被主题遮挡的情况 |

建表完成后，那一行会变成绿色状态：`✅ 词表就绪：实体 208 · 别名 1034 来源：文件：今山 9.json`。
若显示橙色"**尚未导入**"，说明词表为空——此时记忆链不会召回任何往事。
（`今山 9.json` 里有 9 条默认禁用的"解锁包"，它们不会被计入词表。）

- **只做一次**：设置、手动别名、词表都全局共享，换角色、换存档都不用重来。
- 面板「④ 手动别名（可选）」用于补世界书关键词里没有的昵称，每行 `别名 => 实体名`，点「保存别名」。
- 找不到设置面板时（容器 id 不同的宿主），左下角会出现一个 **记忆链** 浮动按钮，点它即可打开同一套设置。

---

## 二·五、剧情闸门（v1.3）

像「【未解锁】落茯区」「【未解锁】九龙洲与九龙港」「【未解锁】理想城」这类条目默认是**关闭**的——它们描述的是"还没发生的事"。闸门负责在你推进剧情时**自动把它们打开**。

**判定规则**：每个里程碑有 `all`（必须全部出现的词）与 `any`（任选其一的词）。每次 AI 回复后，扫描最近 N 轮（默认 4 轮），两者同时满足即判定达成。

**达成后它做什么**：
1. `POST /api/worldinfo/get` 读取当前绑定的世界书；
2. 把目标条目的 `disable` 改成 `false`，并去掉标题里的「【未解锁】」前缀；
3. 改写「**开局状态与剧情进度**」：把"尚未购得／尚未接管"这类句子改成"已于某时…"，并在末尾维护一个「— 已解锁（剧情推进） —」区块；
4. `POST /api/worldinfo/edit` 写回文件，随后**立即重建词表**（解锁后的地名从此可被记忆链召回）。

**默认内置 9 个里程碑**（对应 `今山 9.json` 的 9 个解锁包）：
落茯区购得 · 落茯轩翻新动工 · 落茯券股与落茯快报开办 · 落茯鲜供与落茯站港开办 · 神都之心落成 · 落茯中央银行设立 · 接管月瑶郡·理想城动工 · 购得九龙洲·九龙港 · 海外拓殖。

**两个开关**：
- `启用检测`（默认开）：每轮回复后扫描。
- `自动应用`（**默认关**）：关掉时，检测到的里程碑会出现在面板「待确认的里程碑」里，由你点「应用解锁」；打开则检测到即刻写入。默认关是有意的——**误判一次就会把"还没发生的事"写进书里**。

**里程碑 JSON 格式**（面板「编辑里程碑」可直接改）：
```json
[
  {
    "id": "luofu-land",
    "label": "落茯区购得",
    "all": ["落茯区"],
    "any": ["买下", "购得", "盘下", "成交"],
    "unlock": ["【未解锁】落茯区"],
    "patch": [[
      "· 落茯区：神都西市西城门外那块三角形滩涂**尚未购得**。",
      "· 落茯区：已于{time}购得（营建中）。"
    ]]
  }
]
```
`{time}` 会替换为检测到的现实时间；`patch` 可省略（那就只维护"已解锁"区块）。

**一个注意**：写回的是**磁盘上的世界书文件**，宿主内存里的那份要**刷新页面**才会重载（面板会提示）；扩展自己的词表是即时更新的。

---

## 三、存储（TauriTavern 原生）

扩展会自动按优先级选择后端：

| 优先级 | 后端 | 落盘位置 |
| --- | --- | --- |
| 1 | **TauriTavern 原生 store**（`window.__TAURITAVERN__.api.extension.store`） | `data_root/_tauritavern/extension-store/memory-chain/` |
| 2 | IndexedDB（普通浏览器 / SillyTavern） | 浏览器本地库 `memoryChain` |
| 3 | 内存（不可持久化，面板会黄字提示） | 本次会话有效 |

在 TauriTavern 上的实际文件布局：

```
_tauritavern/extension-store/memory-chain/
  kv/main/global.settings.json        # 参数（全局共享）
  kv/main/global.manualAliases.json   # 手动别名（全局共享）
  kv/main/global.lexicon.json         # 世界书词表（全局共享）
  kv/main/<聊天哈希>.meta.json         # 该聊天的分块索引与统计
  kv/cards.<聊天哈希>/chunk0001.json   # 该聊天的章节卡，每 100 章一个文件
  kv/cards.<聊天哈希>/chunk0002.json
```

> 1.2.0 起：`settings` / `manualAliases` / `lexicon` 用 `global.` 前缀（旧版的按聊天键会在首次读取时自动迁移），只有章节卡与 meta 按聊天哈希隔离。

分块写入的用意：写一章只重写一个分块（≤100 卡），读一整条记忆链只需读 `分块数 + 4` 个文件。
记忆按 **角色 + 存档** 隔离（命名空间 = 角色名 + chat id 的哈希），换档即换链，互不污染。

---

## 四、工作机制

```
用户消息
  ├─ 热区：最近 4 轮原文（每条约 800 字截尾）        ← 模型只看这一段
  ├─ Aho–Corasick 扫描 → 命中实体（O(文本长度)，与词表规模无关）
  ├─ 倒排索引：实体 → 章节卡（每实体上限 3 张）
  ├─ 打分：0.45·相关性 + 0.30·λ^Δ章 + 0.15·log频次 + 0.20·类型 − 0.10·长度
  ├─ min-max 归一化 → 排序 → 预算内裁剪（放不下就降级为摘要）
  ├─ 三态：≤6 章 → 完整；被点名 → 摘要（复活）；≤30 章 → 摘要；更老 → 隐藏
  └─ setExtensionPrompt（对话内 @深度 2）
```

写入侧（不在关键路径）：每 8 轮或点「记录本章」→ `generateQuietPrompt(skipWIAN)` 让**便宜模型**输出 JSON 章节卡 → 入索引 + 落盘。摘要失败自动退回"粗记"。

---

## 五、性能（node 桩环境实测）

| 项目 | 结果 |
| --- | --- |
| 词表构建（201 实体 / 881 别名） | **9.5 ms**（一次性，且会持久化） |
| 单次召回（59 章） | **0.023 ms** |
| 单次召回（5000 章） | **0.024 ms** |
| 写入并索引 5000 章 | **33 ms** |
| 单次注入量 | 554 字（预算 1200，硬顶生效） |

关键点：扫描成本只与文本长度相关；索引全内存，每轮 **0 次 I/O**；只对命中实体算分；同一消息结果有缓存；落盘按分块增量写。

---

## 六、参数

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| 启用 | 开 | 关闭时立刻清空注入 |
| 注入预算（字） | 1200 | **硬上限**；建议 800–1500 |
| 热区轮数 | 4 | 参与实体识别的最近消息数 |
| 时间衰减 λ | 0.85 | 每章保留比例，越小越健忘 |
| 完整注入（章内） | 6 | 距今 ≤ 6 章 → 完整卡 |
| 摘要注入（章内） | 30 | 距今 ≤ 30 章 → 一行；更老则隐藏 |
| 每实体最多卡片 | 3 | 防止一个人带出十条往事 |
| 自动成章（轮） | 8 | 0 = 关闭，改手动 |
| 用模型生成章节卡 | 开 | 关闭则"粗记"，零模型成本 |
| 注入位置 / 深度 | 对话内 / 2 | 亦可改为提示词内或之前 |
| 通用词阈值 | 3 | 某关键词出现在 > 3 个条目里即丢弃 |

---

## 七、排错

| 现象 | 处理 |
| --- | --- |
| 面板黄字"仅内存" | 说明两种持久化后端都不可用；检查是否用了隐私窗口/受限 WebView |
| 一直没有注入 | ① 导入过世界书 JSON？② 已有章节卡？③ 打开「调试日志」看控制台 `[记忆链]` |
| 重启后实体表为空 | 1.1.0 起已随记忆持久化；若为空请重新导入一次世界书 |
| 注入太啰嗦 | 预算降到 800、"完整注入"改 3、每实体卡片改 2 |
| 该带出的旧事没带出 | 补该人物的手动别名，或调大"摘要注入（章内）" |
| 章节切得太碎 | "自动成章"从 8 调到 12–16 |
| 摘要质量差 | 给 quiet prompt 换便宜但中文强的模型；或关掉自动摘要改手动 |

---

## 八、开发

源码目录（不含在本包内）附 `test-harness.mjs`，无需 TauriTavern 即可跑 25 项断言：

```bash
node test-harness.mjs
```

覆盖：词表构建与过滤、实体召回、预算硬顶、时间衰减与三态、5000 章规模化性能、**TauriTavern store 后端的写入/重载复原**。

控制台调试入口：`window.__memoryChain`（`state`、`Store`、`recall()`、`addChapter()`、`buildFromWorldBook()`）。
